import java.util.*;

public class Deck {
    ArrayList<Card> cards = new ArrayList<Card>();
    private int numTerms = 0;

    public Deck(int numTerms, ArrayList<Card> cards) {
        this.numTerms = numTerms;
        this.cards = cards;
    }

    public void addCard(Card card) {

    }
}
